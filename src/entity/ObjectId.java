/* Taken from youtube tutorial
 * Date: January 23, 2017
 * Purpose: Create an ID for each object to differentiate them
 */

package entity;

public enum ObjectId {
	// Predefined Constants used as a library of GameObject Types
	Player(), Block(), BigBlock(), Map(), PlayerP(), PlayerP2(), Enemy(), EnemyBlock(), Portal(), Boss(), BossP(), Spike();

}
